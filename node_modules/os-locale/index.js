'use strict';
var childProcess = require('child_process');
var execFileSync = childProcess.execFileSync;
var lcid = require('lcid');
var defaultOpts = {spawn: true***REMOVED***;
var cache;

function fallback() {
	cache = 'en_US';
	return cache;
***REMOVED***

function getEnvLocale(env) {
	env = env || process.env;
	var ret = env.LC_ALL || env.LC_MESSAGES || env.LANG || env.LANGUAGE;
	cache = getLocale(ret);
	return ret;
***REMOVED***

function parseLocale(x) {
	var env = x.split('\n').reduce(function (env, def) {
		def = def.split('=');
		env[def[0]] = def[1];
		return env;
	***REMOVED***, {***REMOVED***);
	return getEnvLocale(env);
***REMOVED***

function getLocale(str) {
	return (str && str.replace(/[.:].*/, '')) || fallback();
***REMOVED***

module.exports = function (opts, cb) {
	if (typeof opts === 'function') {
		cb = opts;
		opts = defaultOpts;
	***REMOVED*** else {
		opts = opts || defaultOpts;
	***REMOVED***

	if (cache || getEnvLocale() || opts.spawn === false) {
		setImmediate(cb, null, cache);
		return;
	***REMOVED***

	var getAppleLocale = function () {
		childProcess.execFile('defaults', ['read', '-g', 'AppleLocale'], function (err, stdout) {
			if (err) {
				fallback();
				return;
			***REMOVED***

			cache = stdout.trim() || fallback();
			cb(null, cache);
		***REMOVED***);
	***REMOVED***;

	if (process.platform === 'win32') {
		childProcess.execFile('wmic', ['os', 'get', 'locale'], function (err, stdout) {
			if (err) {
				fallback();
				return;
			***REMOVED***

			var lcidCode = parseInt(stdout.replace('Locale', ''), 16);
			cache = lcid.from(lcidCode) || fallback();
			cb(null, cache);
		***REMOVED***);
	***REMOVED*** else {
		childProcess.execFile('locale', function (err, stdout) {
			if (err) {
				fallback();
				return;
			***REMOVED***

			var res = parseLocale(stdout);

			if (!res && process.platform === 'darwin') {
				getAppleLocale();
				return;
			***REMOVED***

			cache = getLocale(res);
			cb(null, cache);
		***REMOVED***);
	***REMOVED***
***REMOVED***;

module.exports.sync = function (opts) {
	opts = opts || defaultOpts;

	if (cache || getEnvLocale() || !execFileSync || opts.spawn === false) {
		return cache;
	***REMOVED***

	if (process.platform === 'win32') {
		var stdout;

		try {
			stdout = execFileSync('wmic', ['os', 'get', 'locale'], {encoding: 'utf8'***REMOVED***);
		***REMOVED*** catch (err) {
			return fallback();
		***REMOVED***

		var lcidCode = parseInt(stdout.replace('Locale', ''), 16);
		cache = lcid.from(lcidCode) || fallback();
		return cache;
	***REMOVED***

	var res;

	try {
		res = parseLocale(execFileSync('locale', {encoding: 'utf8'***REMOVED***));
	***REMOVED*** catch (err) {***REMOVED***

	if (!res && process.platform === 'darwin') {
		try {
			cache = execFileSync('defaults', ['read', '-g', 'AppleLocale'], {encoding: 'utf8'***REMOVED***).trim() || fallback();
			return cache;
		***REMOVED*** catch (err) {
			return fallback();
		***REMOVED***
	***REMOVED***

	cache = getLocale(res);
	return cache;
***REMOVED***;
