import { GRPCCall, GRPCCallOtherArgs, SimpleCallbackFunction ***REMOVED*** from '../apitypes';
import { RetryOptions ***REMOVED*** from '../gax';
/**
 * Creates a function equivalent to func, but that retries on certain
 * exceptions.
 *
 * @private
 *
 * @param {GRPCCall***REMOVED*** func - A function.
 * @param {RetryOptions***REMOVED*** retry - Configures the exceptions upon which the
 *   function eshould retry, and the parameters to the exponential backoff retry
 *   algorithm.
 * @param {GRPCCallOtherArgs***REMOVED*** otherArgs - the additional arguments to be passed to func.
 * @return {SimpleCallbackFunction***REMOVED*** A function that will retry.
 */
export declare function retryable(func: GRPCCall, retry: RetryOptions, otherArgs: GRPCCallOtherArgs): SimpleCallbackFunction;
