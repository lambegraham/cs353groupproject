/**
 * Reverses this ByteBuffer's contents.
 * @param {number=***REMOVED*** begin Offset to start at, defaults to {@link ByteBuffer#offset***REMOVED***
 * @param {number=***REMOVED*** end Offset to end at, defaults to {@link ByteBuffer#limit***REMOVED***
 * @returns {!ByteBuffer***REMOVED*** this
 * @expose
 */
ByteBufferPrototype.reverse = function(begin, end) {
    if (typeof begin === 'undefined') begin = this.offset;
    if (typeof end === 'undefined') end = this.limit;
    if (!this.noAssert) {
        //? ASSERT_RANGE();
  ***REMOVED***
    if (begin === end)
        return this; // Nothing to reverse
    //? if (NODE)
    Array.prototype.reverse.call(this.buffer.slice(begin, end));
    //? else if (DATAVIEW) {
    Array.prototype.reverse.call(new Uint8Array(this.buffer).subarray(begin, end));
    this.view = new DataView(this.buffer); // FIXME: Why exactly is this necessary?
    //? ***REMOVED*** else {
    Array.prototype.reverse.call(this.view.subarray(begin, end));
    //? ***REMOVED***
    return this;
***REMOVED***;
