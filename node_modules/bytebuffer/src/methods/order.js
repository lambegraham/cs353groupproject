/**
 * Sets the byte order.
 * @param {boolean***REMOVED*** littleEndian `true` for little endian byte order, `false` for big endian
 * @returns {!ByteBuffer***REMOVED*** this
 * @expose
 */
ByteBufferPrototype.order = function(littleEndian) {
    if (!this.noAssert) {
        if (typeof littleEndian !== 'boolean')
            throw TypeError("Illegal littleEndian: Not a boolean");
  ***REMOVED***
    this.littleEndian = !!littleEndian;
    return this;
***REMOVED***;

/**
 * Switches (to) little endian byte order.
 * @param {boolean=***REMOVED*** littleEndian Defaults to `true`, otherwise uses big endian
 * @returns {!ByteBuffer***REMOVED*** this
 * @expose
 */
ByteBufferPrototype.LE = function(littleEndian) {
    this.littleEndian = typeof littleEndian !== 'undefined' ? !!littleEndian : true;
    return this;
***REMOVED***;

/**
 * Switches (to) big endian byte order.
 * @param {boolean=***REMOVED*** bigEndian Defaults to `true`, otherwise uses little endian
 * @returns {!ByteBuffer***REMOVED*** this
 * @expose
 */
ByteBufferPrototype.BE = function(bigEndian) {
    this.littleEndian = typeof bigEndian !== 'undefined' ? !bigEndian : false;
    return this;
***REMOVED***;
