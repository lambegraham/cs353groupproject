/**
 * Slices this ByteBuffer by creating a cloned instance with `offset = begin` and `limit = end`.
 * @param {number=***REMOVED*** begin Begin offset, defaults to {@link ByteBuffer#offset***REMOVED***.
 * @param {number=***REMOVED*** end End offset, defaults to {@link ByteBuffer#limit***REMOVED***.
 * @returns {!ByteBuffer***REMOVED*** Clone of this ByteBuffer with slicing applied, backed by the same {@link ByteBuffer#buffer***REMOVED***
 * @expose
 */
ByteBufferPrototype.slice = function(begin, end) {
    if (typeof begin === 'undefined') begin = this.offset;
    if (typeof end === 'undefined') end = this.limit;
    if (!this.noAssert) {
        //? ASSERT_RANGE();
  ***REMOVED***
    var bb = this.clone();
    bb.offset = begin;
    bb.limit = end;
    return bb;
***REMOVED***;
