/**
 * Converts the ByteBuffer's contents to a string.
 * @param {string=***REMOVED*** encoding Output encoding. Returns an informative string representation if omitted but also allows
 *  direct conversion to "utf8", "hex", "base64" and "binary" encoding. "debug" returns a hex representation with
 *  highlighted offsets.
 * @param {number=***REMOVED*** begin Offset to begin at, defaults to {@link ByteBuffer#offset***REMOVED***
 * @param {number=***REMOVED*** end Offset to end at, defaults to {@link ByteBuffer#limit***REMOVED***
 * @returns {string***REMOVED*** String representation
 * @throws {Error***REMOVED*** If `encoding` is invalid
 * @expose
 */
ByteBufferPrototype.toString = function(encoding, begin, end) {
    if (typeof encoding === 'undefined')
        return "ByteBuffer/*?= NODE ? 'NB' : 'AB'+(DATAVIEW ? '_DataView' : '') */(offset="+this.offset+",markedOffset="+this.markedOffset+",limit="+this.limit+",capacity="+this.capacity()+")";
    if (typeof encoding === 'number')
        encoding = "utf8",
        begin = encoding,
        end = begin;
    switch (encoding) {
        //? if (ENCODINGS) {
        //? if (UTF8) {
        case "utf8":
            return this.toUTF8(begin, end);
        //? ***REMOVED*** if (BASE64) {
        case "base64":
            return this.toBase64(begin, end);
        //? ***REMOVED*** if (HEX) {
        case "hex":
            return this.toHex(begin, end);
        //? ***REMOVED*** if (BINARY) {
        case "binary":
            return this.toBinary(begin, end);
        //? ***REMOVED*** if (DEBUG) {
        case "debug":
            return this.toDebug();
        case "columns":
            return this.toColumns();
        //? ***REMOVED***
        //? ***REMOVED*** // ENCODINGS
        default:
            throw Error("Unsupported encoding: "+encoding);
  ***REMOVED***
***REMOVED***;
