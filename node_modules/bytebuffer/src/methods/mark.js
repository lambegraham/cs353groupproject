/**
 * Marks an offset on this ByteBuffer to be used later.
 * @param {number=***REMOVED*** offset Offset to mark. Defaults to {@link ByteBuffer#offset***REMOVED***.
 * @returns {!ByteBuffer***REMOVED*** this
 * @throws {TypeError***REMOVED*** If `offset` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @see ByteBuffer#reset
 * @expose
 */
ByteBufferPrototype.mark = function(offset) {
    offset = typeof offset === 'undefined' ? this.offset : offset;
    if (!this.noAssert) {
        //? ASSERT_OFFSET();
  ***REMOVED***
    this.markedOffset = offset;
    return this;
***REMOVED***;
