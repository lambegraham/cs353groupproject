/**
 * Returns a copy of the backing buffer that contains this ByteBuffer's contents. Contents are the bytes between
 *  {@link ByteBuffer#offset***REMOVED*** and {@link ByteBuffer#limit***REMOVED***.
 * @param {boolean=***REMOVED*** forceCopy If `true` returns a copy, otherwise returns a view referencing the same memory if
 *  possible. Defaults to `false`
//? if (NODE) {
 * @returns {!Buffer***REMOVED*** Contents as a Buffer
//? ***REMOVED*** else {
 * @returns {!ArrayBuffer***REMOVED*** Contents as an ArrayBuffer
//? ***REMOVED***
 * @expose
 */
ByteBufferPrototype.toBuffer = function(forceCopy) {
    var offset = this.offset,
        limit = this.limit;
    if (!this.noAssert) {
        //? ASSERT_RANGE('offset', 'limit');
  ***REMOVED***
    //? if (NODE) {
    if (forceCopy) {
        var buffer = new Buffer(limit - offset);
        this.buffer.copy(buffer, 0, offset, limit);
        return buffer;
  ***REMOVED*** else {
        if (offset === 0 && limit === this.buffer.length)
            return this.buffer;
        else
            return this.buffer.slice(offset, limit);
  ***REMOVED***
    //? ***REMOVED*** else {
    // NOTE: It's not possible to have another ArrayBuffer reference the same memory as the backing buffer. This is
    // possible with Uint8Array#subarray only, but we have to return an ArrayBuffer by contract. So:
    if (!forceCopy && offset === 0 && limit === this.buffer.byteLength)
        return this.buffer;
    if (offset === limit)
        return EMPTY_BUFFER;
    var buffer = new ArrayBuffer(limit - offset);
    new Uint8Array(buffer).set(new Uint8Array(this.buffer).subarray(offset, limit), 0);
    return buffer;
    //? ***REMOVED***
***REMOVED***;

//? if (NODE) {
/**
 * Returns a copy of the backing buffer compacted to contain this ByteBuffer's contents. Contents are the bytes between
 *  {@link ByteBuffer#offset***REMOVED*** and {@link ByteBuffer#limit***REMOVED***.
 * @returns {!ArrayBuffer***REMOVED*** Contents as an ArrayBuffer
 */
ByteBufferPrototype.toArrayBuffer = function() {
    var offset = this.offset,
        limit = this.limit;
    if (!this.noAssert) {
        //? ASSERT_RANGE('offset', 'limit');
  ***REMOVED***
    var ab = new ArrayBuffer(limit - offset);
    if (memcpy) { // Fast
        memcpy(ab, 0, this.buffer, offset, limit);
  ***REMOVED*** else { // Slow
        var dst = new Uint8Array(ab);
        for (var i=offset; i<limit; ++i)
            dst[i-offset] = this.buffer[i];
  ***REMOVED***
    return ab;
***REMOVED***;
//? ***REMOVED*** else {
/**
 * Returns a raw buffer compacted to contain this ByteBuffer's contents. Contents are the bytes between
 *  {@link ByteBuffer#offset***REMOVED*** and {@link ByteBuffer#limit***REMOVED***. This is an alias of {@link ByteBuffer#toBuffer***REMOVED***.
 * @function
 * @param {boolean=***REMOVED*** forceCopy If `true` returns a copy, otherwise returns a view referencing the same memory.
 *  Defaults to `false`
 * @returns {!ArrayBuffer***REMOVED*** Contents as an ArrayBuffer
 * @expose
 */
ByteBufferPrototype.toArrayBuffer = ByteBufferPrototype.toBuffer;
//? ***REMOVED***

