/**
 * Gets the number of remaining readable bytes. Contents are the bytes between {@link ByteBuffer#offset***REMOVED*** and
 *  {@link ByteBuffer#limit***REMOVED***, so this returns `limit - offset`.
 * @returns {number***REMOVED*** Remaining readable bytes. May be negative if `offset > limit`.
 * @expose
 */
ByteBufferPrototype.remaining = function() {
    return this.limit - this.offset;
***REMOVED***;
