// encodings/impl/binary

/**
 * Encodes a binary JavaScript string to bytes.
 * @param {string***REMOVED*** src Source string
 * @param {number***REMOVED*** srcOffset Source offset
 * @param {!ByteBuffer***REMOVED*** dst Destination ByteBuffer
 * @param {number***REMOVED*** dstOffset Destination offset
 * @param {number***REMOVED*** count Number of char codes to encode
 * @returns {number***REMOVED*** Number of bytes encoded
 * @inner
 */
function binary_encode(src, srcOffset, dst, dstOffset, count) {
    var n = 0;
    while (count--) {
        var cc = src.charCodeAt(srcOffset++);
        if (cc > 255)
            throw Error("illegal binary char code: "+cc);
        //? SET('cc', 'dstOffset++', 'dst');
        ++n;
  ***REMOVED***
    return n;
***REMOVED***

/**
 * Decodes bytes to a binary JavaScript string.
 * @param {!ByteBuffer***REMOVED*** src Source ByteBuffer
 * @param {number***REMOVED*** srcOffset Source offset
 * @param {number***REMOVED*** count Number of bytes to decode
 * @returns {string***REMOVED*** Decoded string
 * @inner
 */
function binary_decode(src, srcOffset, count) {
    if (count === 0)
        return "";
    var parts = [], // readily assembled parts
        batch = []; // char codes for batch processing
    while (count--) {
        batch.push(/*? GET('srcOffset++', 'src') */);
        if (batch.length > 1023) {
            parts.push(String.fromCharCode.apply(String, batch));
            batch.length = 0;
      ***REMOVED***
  ***REMOVED***
    if (batch.length > 0) {
        if (parts.length === 0)
            return String.fromCharCode.apply(String, batch);
        parts.push(String.fromCharCode.apply(String, batch));
  ***REMOVED***
    return parts.join('');
***REMOVED***

/**
 * Calculates the number of bytes required to store a binary JavaScript string.
 * @param {string***REMOVED*** src Source string
 * @param {number***REMOVED*** srcOffset Source offset
 * @param {number***REMOVED*** count Number of char codes to calculate
 * @returns {number***REMOVED*** Number of bytes required
 * @inner
 */
function binary_calculate(src, srcOffset, count) {
    return count;
***REMOVED***

ByteBuffer.registerEncoding("binary", binary_encode, binary_decode, binary_calculate);
