//? if (INT16) {
// types/ints/int16

/**
 * Writes a 16bit signed integer.
 * @param {number***REMOVED*** value Value to write
 * @param {number=***REMOVED*** offset Offset to write to. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @throws {TypeError***REMOVED*** If `offset` or `value` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.writeInt16 = function(value, offset) {
    //? RELATIVE();
    if (!this.noAssert) {
        //? ASSERT_INTEGER('value');
        //? ASSERT_OFFSET();
  ***REMOVED***
    //? ENSURE_CAPACITY(2);
    //? if (NODE || !DATAVIEW) { var dst = NODE ? 'this.buffer' : 'this.view';
    if (this.littleEndian) {
        /*?= dst */[offset+1] = (value & 0xFF00) >>> 8;
        /*?= dst */[offset  ] =  value & 0x00FF;
  ***REMOVED*** else {
        /*?= dst */[offset]   = (value & 0xFF00) >>> 8;
        /*?= dst */[offset+1] =  value & 0x00FF;
  ***REMOVED***
    //? ***REMOVED*** else
    this.view.setInt16(offset, value, this.littleEndian);
    //? RELATIVE(2);
    return this;
***REMOVED***;
//? if (ALIASES) {

/**
 * Writes a 16bit signed integer. This is an alias of {@link ByteBuffer#writeInt16***REMOVED***.
 * @function
 * @param {number***REMOVED*** value Value to write
 * @param {number=***REMOVED*** offset Offset to write to. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @throws {TypeError***REMOVED*** If `offset` or `value` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.writeShort = ByteBufferPrototype.writeInt16;
//? ***REMOVED***

/**
 * Reads a 16bit signed integer.
 * @param {number=***REMOVED*** offset Offset to read from. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @returns {number***REMOVED*** Value read
 * @throws {TypeError***REMOVED*** If `offset` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.readInt16 = function(offset) {
    //? RELATIVE();
    if (!this.noAssert) {
        //? ASSERT_OFFSET(2);
  ***REMOVED***
    //? if (NODE || !DATAVIEW) { var dst = NODE ? 'this.buffer' : 'this.view';
    var value = 0;
    if (this.littleEndian) {
        value  = /*?= dst */[offset  ];
        value |= /*?= dst */[offset+1] << 8;
  ***REMOVED*** else {
        value  = /*?= dst */[offset  ] << 8;
        value |= /*?= dst */[offset+1];
  ***REMOVED***
    if ((value & 0x8000) === 0x8000) value = -(0xFFFF - value + 1); // Cast to signed
    //? ***REMOVED*** else
    var value = this.view.getInt16(offset, this.littleEndian);
    //? RELATIVE(2);
    return value;
***REMOVED***;
//? if (ALIASES) {

/**
 * Reads a 16bit signed integer. This is an alias of {@link ByteBuffer#readInt16***REMOVED***.
 * @function
 * @param {number=***REMOVED*** offset Offset to read from. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @returns {number***REMOVED*** Value read
 * @throws {TypeError***REMOVED*** If `offset` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.readShort = ByteBufferPrototype.readInt16;
//? ***REMOVED***

/**
 * Writes a 16bit unsigned integer.
 * @param {number***REMOVED*** value Value to write
 * @param {number=***REMOVED*** offset Offset to write to. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @throws {TypeError***REMOVED*** If `offset` or `value` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.writeUint16 = function(value, offset) {
    //? RELATIVE();
    if (!this.noAssert) {
        //? ASSERT_INTEGER('value', true);
        //? ASSERT_OFFSET();
  ***REMOVED***
    //? ENSURE_CAPACITY(2);
    //? if (NODE || !DATAVIEW) { var dst = NODE ? 'this.buffer' : 'this.view';
    if (this.littleEndian) {
        /*?= dst */[offset+1] = (value & 0xFF00) >>> 8;
        /*?= dst */[offset  ] =  value & 0x00FF;
  ***REMOVED*** else {
        /*?= dst */[offset]   = (value & 0xFF00) >>> 8;
        /*?= dst */[offset+1] =  value & 0x00FF;
  ***REMOVED***
    //? ***REMOVED*** else
    this.view.setUint16(offset, value, this.littleEndian);
    //? RELATIVE(2);
    return this;
***REMOVED***;

/**
 * Writes a 16bit unsigned integer. This is an alias of {@link ByteBuffer#writeUint16***REMOVED***.
 * @function
 * @param {number***REMOVED*** value Value to write
 * @param {number=***REMOVED*** offset Offset to write to. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @throws {TypeError***REMOVED*** If `offset` or `value` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.writeUInt16 = ByteBufferPrototype.writeUint16;

/**
 * Reads a 16bit unsigned integer.
 * @param {number=***REMOVED*** offset Offset to read from. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @returns {number***REMOVED*** Value read
 * @throws {TypeError***REMOVED*** If `offset` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.readUint16 = function(offset) {
    //? RELATIVE();
    if (!this.noAssert) {
        //? ASSERT_OFFSET(2);
  ***REMOVED***
    //? if (NODE || !DATAVIEW) { var dst = NODE ? 'this.buffer' : 'this.view';
    var value = 0;
    if (this.littleEndian) {
        value  = /*?= dst */[offset  ];
        value |= /*?= dst */[offset+1] << 8;
  ***REMOVED*** else {
        value  = /*?= dst */[offset  ] << 8;
        value |= /*?= dst */[offset+1];
  ***REMOVED***
    //? ***REMOVED*** else
    var value = this.view.getUint16(offset, this.littleEndian);
    //? RELATIVE(2);
    return value;
***REMOVED***;

/**
 * Reads a 16bit unsigned integer. This is an alias of {@link ByteBuffer#readUint16***REMOVED***.
 * @function
 * @param {number=***REMOVED*** offset Offset to read from. Will use and advance {@link ByteBuffer#offset***REMOVED*** by `2` if omitted.
 * @returns {number***REMOVED*** Value read
 * @throws {TypeError***REMOVED*** If `offset` is not a valid number
 * @throws {RangeError***REMOVED*** If `offset` is out of bounds
 * @expose
 */
ByteBufferPrototype.readUInt16 = ByteBufferPrototype.readUint16;

//? ***REMOVED***