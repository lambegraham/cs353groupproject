//? if (INT64) {
// types/ints/int64

if (Long) {

    /**
     * Writes a 64bit signed integer.
     * @param {number|!Long***REMOVED*** value Value to write
     * @param {number=***REMOVED*** offset Offset to write to. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!ByteBuffer***REMOVED*** this
     * @expose
     */
    ByteBufferPrototype.writeInt64 = function(value, offset) {
        //? RELATIVE();
        if (!this.noAssert) {
            //? ASSERT_LONG('value');
            //? ASSERT_OFFSET();
      ***REMOVED***
        //? LONG('value');
        //? ENSURE_CAPACITY(8);
        //? if (NODE || !DATAVIEW) {
        var lo = value.low,
            hi = value.high;
        if (this.littleEndian) {
            //? WRITE_UINT32_ARRAY('lo', undefined, undefined, true);
            offset += 4;
            //? WRITE_UINT32_ARRAY('hi', undefined, undefined, true);
      ***REMOVED*** else {
            //? WRITE_UINT32_ARRAY('hi', undefined, undefined, false);
            offset += 4;
            //? WRITE_UINT32_ARRAY('lo', undefined, undefined, false);
      ***REMOVED***
        //? ***REMOVED*** else {
        if (this.littleEndian) {
            this.view.setInt32(offset  , value.low , true);
            this.view.setInt32(offset+4, value.high, true);
      ***REMOVED*** else {
            this.view.setInt32(offset  , value.high, false);
            this.view.setInt32(offset+4, value.low , false);
      ***REMOVED***
        //? ***REMOVED***
        //? RELATIVE(8);
        return this;
  ***REMOVED***
    //? if (ALIASES) {

    /**
     * Writes a 64bit signed integer. This is an alias of {@link ByteBuffer#writeInt64***REMOVED***.
     * @param {number|!Long***REMOVED*** value Value to write
     * @param {number=***REMOVED*** offset Offset to write to. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!ByteBuffer***REMOVED*** this
     * @expose
     */
    ByteBufferPrototype.writeLong = ByteBufferPrototype.writeInt64;
    //? ***REMOVED***
    
    /**
     * Reads a 64bit signed integer.
     * @param {number=***REMOVED*** offset Offset to read from. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!Long***REMOVED***
     * @expose
     */
    ByteBufferPrototype.readInt64 = function(offset) {
        //? RELATIVE();
        if (!this.noAssert) {
            //? ASSERT_OFFSET(8);
      ***REMOVED***
        //? if (NODE || !DATAVIEW) {
        var lo = 0,
            hi = 0;
        if (this.littleEndian) {
            //? READ_UINT32_ARRAY('lo', undefined, undefined, true);
            offset += 4;
            //? READ_UINT32_ARRAY('hi', undefined, undefined, true);
      ***REMOVED*** else {
            //? READ_UINT32_ARRAY('hi', undefined, undefined, false);
            offset += 4;
            //? READ_UINT32_ARRAY('lo', undefined, undefined, false);
      ***REMOVED***
        var value = new Long(lo, hi, false);
        //? ***REMOVED*** else {
        var value = this.littleEndian
            ? new Long(this.view.getInt32(offset  , true ), this.view.getInt32(offset+4, true ), false)
            : new Long(this.view.getInt32(offset+4, false), this.view.getInt32(offset  , false), false);
        //? ***REMOVED***
        //? RELATIVE(8);
        return value;
  ***REMOVED***
    //? if (ALIASES) {

    /**
     * Reads a 64bit signed integer. This is an alias of {@link ByteBuffer#readInt64***REMOVED***.
     * @param {number=***REMOVED*** offset Offset to read from. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!Long***REMOVED***
     * @expose
     */
    ByteBufferPrototype.readLong = ByteBufferPrototype.readInt64;
    //? ***REMOVED***
    
    /**
     * Writes a 64bit unsigned integer.
     * @param {number|!Long***REMOVED*** value Value to write
     * @param {number=***REMOVED*** offset Offset to write to. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!ByteBuffer***REMOVED*** this
     * @expose
     */
    ByteBufferPrototype.writeUint64 = function(value, offset) {
        //? RELATIVE();
        if (!this.noAssert) {
            //? ASSERT_LONG('value');
            //? ASSERT_OFFSET();
      ***REMOVED***
        //? LONG('value');
        //? ENSURE_CAPACITY(8);
        //? if (NODE || !DATAVIEW) {
        var lo = value.low,
            hi = value.high;
        if (this.littleEndian) {
            //? WRITE_UINT32_ARRAY('lo', undefined, undefined, true);
            offset += 4;
            //? WRITE_UINT32_ARRAY('hi', undefined, undefined, true);
      ***REMOVED*** else {
            //? WRITE_UINT32_ARRAY('hi', undefined, undefined, false);
            offset += 4;
            //? WRITE_UINT32_ARRAY('lo', undefined, undefined, false);
      ***REMOVED***
        //? ***REMOVED*** else {
        if (this.littleEndian) {
            this.view.setInt32(offset  , value.low , true);
            this.view.setInt32(offset+4, value.high, true);
      ***REMOVED*** else {
            this.view.setInt32(offset  , value.high, false);
            this.view.setInt32(offset+4, value.low , false);
      ***REMOVED***
        //? ***REMOVED***
        //? RELATIVE(8);
        return this;
  ***REMOVED***

    /**
     * Writes a 64bit unsigned integer. This is an alias of {@link ByteBuffer#writeUint64***REMOVED***.
     * @function
     * @param {number|!Long***REMOVED*** value Value to write
     * @param {number=***REMOVED*** offset Offset to write to. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!ByteBuffer***REMOVED*** this
     * @expose
     */
    ByteBufferPrototype.writeUInt64 = ByteBufferPrototype.writeUint64;
    
    /**
     * Reads a 64bit unsigned integer.
     * @param {number=***REMOVED*** offset Offset to read from. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!Long***REMOVED***
     * @expose
     */
    ByteBufferPrototype.readUint64 = function(offset) {
        //? RELATIVE();
        if (!this.noAssert) {
            //? ASSERT_OFFSET(8);
      ***REMOVED***
        //? if (NODE || !DATAVIEW) {
        var lo = 0,
            hi = 0;
        if (this.littleEndian) {
            //? READ_UINT32_ARRAY('lo', undefined, undefined, true);
            offset += 4;
            //? READ_UINT32_ARRAY('hi', undefined, undefined, true);
      ***REMOVED*** else {
            //? READ_UINT32_ARRAY('hi', undefined, undefined, false);
            offset += 4;
            //? READ_UINT32_ARRAY('lo', undefined, undefined, false);
      ***REMOVED***
        var value = new Long(lo, hi, true);
        //? ***REMOVED*** else {
        var value = this.littleEndian
            ? new Long(this.view.getInt32(offset  , true ), this.view.getInt32(offset+4, true ), true)
            : new Long(this.view.getInt32(offset+4, false), this.view.getInt32(offset  , false), true);
        //? ***REMOVED***
        //? RELATIVE(8);
        return value;
  ***REMOVED***

    /**
     * Reads a 64bit unsigned integer. This is an alias of {@link ByteBuffer#readUint64***REMOVED***.
     * @function
     * @param {number=***REMOVED*** offset Offset to read from. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
     * @returns {!Long***REMOVED***
     * @expose
     */
    ByteBufferPrototype.readUInt64 = ByteBufferPrototype.readUint64;
    
***REMOVED*** // Long

//? ***REMOVED***