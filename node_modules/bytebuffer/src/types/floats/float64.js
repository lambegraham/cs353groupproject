//? if (FLOAT64) {
// types/floats/float64

/**
 * Writes a 64bit float.
 * @param {number***REMOVED*** value Value to write
 * @param {number=***REMOVED*** offset Offset to write to. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
 * @returns {!ByteBuffer***REMOVED*** this
 * @expose
 */
ByteBufferPrototype.writeFloat64 = function(value, offset) {
    //? RELATIVE();
    if (!this.noAssert) {
        if (typeof value !== 'number')
            throw TypeError("Illegal value: "+value+" (not a number)");
        //? ASSERT_OFFSET();
  ***REMOVED***
    //? ENSURE_CAPACITY(8);
    //? if (NODE) {
    this.littleEndian
        ? this.buffer.writeDoubleLE(value, offset, true)
        : this.buffer.writeDoubleBE(value, offset, true);
    //? ***REMOVED*** else if (DATAVIEW)
    this.view.setFloat64(offset, value, this.littleEndian);
    //? else
    ieee754_write(this.view, value, offset, this.littleEndian, 52, 8);
    //? RELATIVE(8);
    return this;
***REMOVED***;
//? if (ALIASES) {

/**
 * Writes a 64bit float. This is an alias of {@link ByteBuffer#writeFloat64***REMOVED***.
 * @function
 * @param {number***REMOVED*** value Value to write
 * @param {number=***REMOVED*** offset Offset to write to. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
 * @returns {!ByteBuffer***REMOVED*** this
 * @expose
 */
ByteBufferPrototype.writeDouble = ByteBufferPrototype.writeFloat64;
//? ***REMOVED***

/**
 * Reads a 64bit float.
 * @param {number=***REMOVED*** offset Offset to read from. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
 * @returns {number***REMOVED***
 * @expose
 */
ByteBufferPrototype.readFloat64 = function(offset) {
    //? RELATIVE();
    if (!this.noAssert) {
        //? ASSERT_OFFSET(8);
  ***REMOVED***
    //? if (NODE) {
    var value = this.littleEndian
        ? this.buffer.readDoubleLE(offset, true)
        : this.buffer.readDoubleBE(offset, true);
    //? ***REMOVED*** else if (DATAVIEW)
    var value = this.view.getFloat64(offset, this.littleEndian);
    //? else
    var value = ieee754_read(this.view, offset, this.littleEndian, 52, 8);
    //? RELATIVE(8);
    return value;
***REMOVED***;
//? if (ALIASES) {

/**
 * Reads a 64bit float. This is an alias of {@link ByteBuffer#readFloat64***REMOVED***.
 * @function
 * @param {number=***REMOVED*** offset Offset to read from. Will use and increase {@link ByteBuffer#offset***REMOVED*** by `8` if omitted.
 * @returns {number***REMOVED***
 * @expose
 */
ByteBufferPrototype.readDouble = ByteBufferPrototype.readFloat64;
//? ***REMOVED***

//? ***REMOVED***