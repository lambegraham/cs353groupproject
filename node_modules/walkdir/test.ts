import * as walkdir from './walkdir';

let a = walkdir('aaa',{sync:true,return_object:true***REMOVED***,function(path){
    this.ignore(path)
***REMOVED***)
a[Object.keys(a)[0]].isDirectory()

let b = walkdir('aaa',{sync:true***REMOVED***,function(path){
    this.ignore(path)
***REMOVED***)
b.splice(0)

let c = walkdir('bbb')
c.emit('a')
c.pause()
c.resume()
c.end()


let d = walkdir('ccc',{***REMOVED***,(path)=>{
    path.substr
***REMOVED***)
d.emit('a')

let e = walkdir('ddd',(path)=>{
    path.substr
***REMOVED***)
e.emit('a')


let f = walkdir('bbb',{sync:true***REMOVED***)
f.splice(0)


walkdir.async("").then((s)=>s.splice(0))

walkdir.async("",{***REMOVED***).then((s)=>s.splice(0))

walkdir.async("",{return_object:true***REMOVED***).then((a)=>{
    a[Object.keys(a)[0]].isDirectory()
***REMOVED***)

let g = walkdir.find('')
g.emit('a')

let g1 = walkdir.find('',()=>{***REMOVED***)
g1.emit('a')

let g2 = walkdir.find('',{***REMOVED***,()=>{***REMOVED***)
g2.emit('a')

let h = walkdir.find('',{sync:true***REMOVED***)
h.splice(0)

let i = walkdir.find('',{sync:true,return_object:true***REMOVED***)
i[Object.keys(i)[0]].isDirectory()


let j = walkdir.sync('')
j.splice(0)

let k = walkdir.sync('',{return_object:true***REMOVED***)
k[Object.keys(k)[0]].isDirectory()

let l = walkdir.sync('',(path)=>{
    path.substr
***REMOVED***)
l.splice(0)
