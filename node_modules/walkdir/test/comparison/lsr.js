var lsr = require('ls-r');

lsr(process.argv[2]||'./',{maxDepth:500000,recursive:true***REMOVED***,function(err,origPath,args){
	if(err) {
		console.log('eww an error! ',err);
		return;
	***REMOVED***
//console.log('hit');
	var c = 0;
	args.forEach(function(stat){
		if(stat.isFile()){
			console.log(stat.path);
			c++;
		***REMOVED***
	***REMOVED***);

	console.log('found '+args.length+" regular files");
***REMOVED***);
