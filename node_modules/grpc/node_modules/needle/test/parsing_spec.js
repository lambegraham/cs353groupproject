var should = require('should'),
    needle = require('./../'),
    http   = require('http'),
    port   = 11111,
    server;

describe('parsing', function(){

  describe('when response is an JSON string', function(){

    var json_string = '{"foo":"bar"***REMOVED***';

    before(function(done){
      server = http.createServer(function(req, res) {
        res.setHeader('Content-Type', 'application/json');
        res.end(json_string);
    ***REMOVED***).listen(port, done);
  ***REMOVED***);

    after(function(done){
      server.close(done);
  ***REMOVED***)

    describe('and parse option is not passed', function() {

      describe('with default parse_response', function() {

        before(function() {
          needle.defaults().parse_response.should.eql('all')
      ***REMOVED***)

        it('should return object', function(done){
          needle.get('localhost:' + port, function(err, response, body){
            should.ifError(err);
            body.should.have.property('foo', 'bar');
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***)

      describe('and default parse_response is set to false', function() {

        it('does NOT return object when disabled using .defaults', function(done){
          needle.defaults({ parse_response: false ***REMOVED***)

          needle.get('localhost:' + port, function(err, response, body) {
            should.not.exist(err);
            body.should.be.an.instanceof(Buffer)
            body.toString().should.eql('{"foo":"bar"***REMOVED***');

            needle.defaults({ parse_response: 'all' ***REMOVED***);
            done();
        ***REMOVED***)
      ***REMOVED***)


    ***REMOVED***)

  ***REMOVED***)

    describe('and parse option is true', function() {

      describe('and JSON is valid', function() {

        it('should return object', function(done) {
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, response, body){
            should.not.exist(err);
            body.should.have.property('foo', 'bar')
            done();
        ***REMOVED***)
      ***REMOVED***)

        it('should have a .parser = json property', function(done) {
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, resp) {
            should.not.exist(err);
            resp.parser.should.eql('json');
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***);

      describe('and response is empty', function() {

        var old_json_string;

        before(function() {
          old_json_string = json_string;
          json_string = "";
      ***REMOVED***);

        after(function() {
          json_string = old_json_string;
      ***REMOVED***);

        it('should return an empty string', function(done) {
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, resp) {
            should.not.exist(err);
            resp.body.should.equal('');
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***)

      describe('and JSON is invalid', function() {

        var old_json_string;

        before(function() {
          old_json_string = json_string;
          json_string = "this is not going to work";
      ***REMOVED***);

        after(function() {
          json_string = old_json_string;
      ***REMOVED***);

        it('does not throw', function(done) {
          (function(){
            needle.get('localhost:' + port, { parse: true ***REMOVED***, done);
        ***REMOVED***).should.not.throw();
      ***REMOVED***);

        it('does NOT return object', function(done) {
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, response, body) {
            should.not.exist(err);
            body.should.be.a.String;
            body.toString().should.eql('this is not going to work');
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***);

  ***REMOVED***)

    describe('and parse option is false', function() {

      it('does NOT return object', function(done){
        needle.get('localhost:' + port, { parse: false ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.should.be.an.instanceof(Buffer)
          body.toString().should.eql('{"foo":"bar"***REMOVED***');
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should NOT have a .parser = json property', function(done) {
        needle.get('localhost:' + port, { parse: false ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          should.not.exist(resp.parser);
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

    describe('and parse option is "xml"', function() {

      it('does NOT return object', function(done){
        needle.get('localhost:' + port, { parse: 'xml' ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.should.be.an.instanceof(Buffer)
          body.toString().should.eql('{"foo":"bar"***REMOVED***');
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should NOT have a .parser = json property', function(done) {
        needle.get('localhost:' + port, { parse: 'xml' ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          should.not.exist(resp.parser);
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

***REMOVED***);

  describe('when response is JSON \'false\'', function(){

    var json_string = 'false';

    before(function(done){
      server = http.createServer(function(req, res) {
        res.setHeader('Content-Type', 'application/json');
        res.end(json_string);
    ***REMOVED***).listen(port, done);
  ***REMOVED***);

    after(function(done){
      server.close(done);
  ***REMOVED***)

    describe('and parse option is not passed', function() {

      it('should return object', function(done){
        needle.get('localhost:' + port, function(err, response, body){
          should.ifError(err);
          body.should.equal(false);
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

    describe('and parse option is true', function() {

      describe('and JSON is valid', function() {

        it('should return object', function(done){
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, response, body){
            should.not.exist(err);
            body.should.equal(false)
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***);

      describe('and response is empty', function() {

        var old_json_string;

        before(function() {
          old_json_string = json_string;
          json_string = "";
      ***REMOVED***);

        after(function() {
          json_string = old_json_string;
      ***REMOVED***);

        it('should return an empty string', function(done) {
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, resp) {
            should.not.exist(err);
            resp.body.should.equal('');
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***)

      describe('and JSON is invalid', function() {

        var old_json_string;

        before(function() {
          old_json_string = json_string;
          json_string = "this is not going to work";
      ***REMOVED***);

        after(function() {
          json_string = old_json_string;
      ***REMOVED***);

        it('does not throw', function(done) {
          (function(){
            needle.get('localhost:' + port, { parse: true ***REMOVED***, done);
        ***REMOVED***).should.not.throw();
      ***REMOVED***);

        it('does NOT return object', function(done) {
          needle.get('localhost:' + port, { parse: true ***REMOVED***, function(err, response, body) {
            should.not.exist(err);
            body.should.be.a.String;
            body.toString().should.eql('this is not going to work');
            done();
        ***REMOVED***)
      ***REMOVED***)

    ***REMOVED***);

  ***REMOVED***)

    describe('and parse option is false', function() {

      it('does NOT return object', function(done){
        needle.get('localhost:' + port, { parse: false ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.should.be.an.instanceof(Buffer)
          body.toString().should.eql('false');
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

    describe('and parse option is "xml"', function() {

      it('does NOT return object', function(done){
        needle.get('localhost:' + port, { parse: 'xml' ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.should.be.an.instanceof(Buffer)
          body.toString().should.eql('false');
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)


***REMOVED***);

  describe('when response is an invalid XML string', function(){

    before(function(done){
      server = http.createServer(function(req, res) {
        res.writeHeader(200, {'Content-Type': 'application/xml'***REMOVED***)
        res.end("<post><body there11post>")
    ***REMOVED***).listen(port, done);
  ***REMOVED***);

    after(function(done){
      server.close(done);
  ***REMOVED***)

    describe('and parse_response is true', function(){

      it('should return original string', function(done) {
        needle.get('localhost:' + port, { parse_response: true ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.should.eql('<post><body there11post>')
          should.not.exist(body.name);
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should not have a .parser = xml property', function(done) {
        needle.get('localhost:' + port, { parse_response: true ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          should.not.exist(resp.parser);
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

    describe('and parse response is false', function(){

      it('should return valid object', function(done) {
        needle.get('localhost:' + port, { parse_response: false ***REMOVED***, function(err, response, body){
          should.not.exist(err);
          body.toString().should.eql('<post><body there11post>')
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should not have a .parser property', function(done) {
        needle.get('localhost:' + port, { parse_response: false ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          should.not.exist(resp.parser)
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

***REMOVED***)

  describe('when response is a valid XML string', function(){

    before(function(done) {
      server = http.createServer(function(req, res) {
        res.writeHeader(200, {'Content-Type': 'application/xml'***REMOVED***)
        res.end("<post><p>hello</p><p>world</p></post>")
    ***REMOVED***).listen(port, done);
  ***REMOVED***);

    after(function(done) {
      server.close(done);
  ***REMOVED***)

    describe('and parse_response is true', function(){

      it('should return valid object', function(done) {
        needle.get('localhost:' + port, { parse_response: true ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.name.should.eql('post')
          body.children[0].name.should.eql('p')
          body.children[0].value.should.eql('hello')

          body.children[1].name.should.eql('p')
          body.children[1].value.should.eql('world')
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should have a .parser = xml property', function(done) {
        needle.get('localhost:' + port, { parse_response: true ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          resp.parser.should.eql('xml');
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

    describe('and parse response is false', function(){

      it('should return valid object', function(done) {
        needle.get('localhost:' + port, { parse_response: false ***REMOVED***, function(err, response, body){
          should.not.exist(err);
          body.toString().should.eql('<post><p>hello</p><p>world</p></post>')
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should not have a .parser property', function(done) {
        needle.get('localhost:' + port, { parse_response: false ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          should.not.exist(resp.parser)
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

***REMOVED***)


  describe('valid XML, using xml2js', function() {

    var parsers, origParser;

    before(function(done) {
      var xml2js = require('xml2js')
      parsers = require('../lib/parsers');
      origParser = parsers['application/xml'];

      var customParser = require('xml2js').parseString;
      parsers.use('xml2js', ['application/xml'], function(buff, cb) {
        var opts = { explicitRoot: true, explicitArray: false ***REMOVED***;
        customParser(buff, opts, cb);
    ***REMOVED***)

      server = http.createServer(function(req, res) {
        res.writeHeader(200, {'Content-Type': 'application/xml'***REMOVED***)
        res.end("<post><p>hello</p><p>world</p></post>")
    ***REMOVED***).listen(port, done);
  ***REMOVED***);

    after(function(done) {
      parsers['application/xml'] = origParser;
      server.close(done);
  ***REMOVED***)

    describe('and parse_response is true', function(){

      it('should return valid object', function(done) {
        needle.get('localhost:' + port, { parse_response: true ***REMOVED***, function(err, response, body) {
          should.not.exist(err);
          body.should.eql({ post: { p: ['hello', 'world' ]***REMOVED******REMOVED***)
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should have a .parser = xml property', function(done) {
        needle.get('localhost:' + port, { parse_response: true ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          resp.parser.should.eql('xml2js');
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

    describe('and parse response is false', function(){

      it('should return valid object', function(done) {
        needle.get('localhost:' + port, { parse_response: false ***REMOVED***, function(err, response, body){
          should.not.exist(err);
          body.toString().should.eql('<post><p>hello</p><p>world</p></post>')
          done();
      ***REMOVED***)
    ***REMOVED***)

      it('should not have a .parser property', function(done) {
        needle.get('localhost:' + port, { parse_response: false ***REMOVED***, function(err, resp) {
          should.not.exist(err);
          should.not.exist(resp.parser)
          done();
      ***REMOVED***)
    ***REMOVED***)

  ***REMOVED***)

***REMOVED***)


***REMOVED***)
