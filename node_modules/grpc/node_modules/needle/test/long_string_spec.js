var needle = require('../'),
    should = require('should');

describe('when posting a very long string', function() {

  this.timeout(20000);

  function get_string(length) {
    var str = '';
    for (var i = 0; i < length; i++) {
      str += 'x';
  ***REMOVED***
    return str;
***REMOVED***

  it("shouldn't throw an EPIPE error out of nowhere", function(done) {
    var error;

    function finished() {
      setTimeout(function() {
        should.not.exist(error);
        done();
    ***REMOVED***, 300);
  ***REMOVED***

    try {
      needle.post('https://google.com', { data: get_string(Math.pow(2, 20)) ***REMOVED***, finished)
  ***REMOVED*** catch(e) {
      error = e;
  ***REMOVED***

***REMOVED***)

***REMOVED***)
