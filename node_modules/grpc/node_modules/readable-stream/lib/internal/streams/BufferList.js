'use strict';

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); ***REMOVED*** ***REMOVED***

var Buffer = require('safe-buffer').Buffer;
var util = require('util');

function copyBuffer(src, target, offset) {
  src.copy(target, offset);
***REMOVED***

module.exports = function () {
  function BufferList() {
    _classCallCheck(this, BufferList);

    this.head = null;
    this.tail = null;
    this.length = 0;
***REMOVED***

  BufferList.prototype.push = function push(v) {
    var entry = { data: v, next: null ***REMOVED***;
    if (this.length > 0) this.tail.next = entry;else this.head = entry;
    this.tail = entry;
    ++this.length;
***REMOVED***

  BufferList.prototype.unshift = function unshift(v) {
    var entry = { data: v, next: this.head ***REMOVED***;
    if (this.length === 0) this.tail = entry;
    this.head = entry;
    ++this.length;
***REMOVED***

  BufferList.prototype.shift = function shift() {
    if (this.length === 0) return;
    var ret = this.head.data;
    if (this.length === 1) this.head = this.tail = null;else this.head = this.head.next;
    --this.length;
    return ret;
***REMOVED***

  BufferList.prototype.clear = function clear() {
    this.head = this.tail = null;
    this.length = 0;
***REMOVED***

  BufferList.prototype.join = function join(s) {
    if (this.length === 0) return '';
    var p = this.head;
    var ret = '' + p.data;
    while (p = p.next) {
      ret += s + p.data;
  ***REMOVED***return ret;
***REMOVED***

  BufferList.prototype.concat = function concat(n) {
    if (this.length === 0) return Buffer.alloc(0);
    if (this.length === 1) return this.head.data;
    var ret = Buffer.allocUnsafe(n >>> 0);
    var p = this.head;
    var i = 0;
    while (p) {
      copyBuffer(p.data, ret, i);
      i += p.data.length;
      p = p.next;
  ***REMOVED***
    return ret;
***REMOVED***

  return BufferList;
***REMOVED***();

if (util && util.inspect && util.inspect.custom) {
  module.exports.prototype[util.inspect.custom] = function () {
    var obj = util.inspect({ length: this.length ***REMOVED***);
    return this.constructor.name + ' ' + obj;
***REMOVED***
***REMOVED***