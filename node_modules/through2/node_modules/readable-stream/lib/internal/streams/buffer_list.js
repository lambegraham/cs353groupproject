'use strict';

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {***REMOVED***; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; ***REMOVED***)); ***REMOVED*** ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); ***REMOVED***); ***REMOVED*** return target; ***REMOVED***

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true ***REMOVED***); ***REMOVED*** else { obj[key] = value; ***REMOVED*** return obj; ***REMOVED***

var _require = require('buffer'),
    Buffer = _require.Buffer;

var _require2 = require('util'),
    inspect = _require2.inspect;

var custom = inspect && inspect.custom || 'inspect';

function copyBuffer(src, target, offset) {
  Buffer.prototype.copy.call(src, target, offset);
***REMOVED***

module.exports =
/*#__PURE__*/
function () {
  function BufferList() {
    this.head = null;
    this.tail = null;
    this.length = 0;
***REMOVED***

  var _proto = BufferList.prototype;

  _proto.push = function push(v) {
    var entry = {
      data: v,
      next: null
  ***REMOVED***
    if (this.length > 0) this.tail.next = entry;else this.head = entry;
    this.tail = entry;
    ++this.length;
***REMOVED***

  _proto.unshift = function unshift(v) {
    var entry = {
      data: v,
      next: this.head
  ***REMOVED***
    if (this.length === 0) this.tail = entry;
    this.head = entry;
    ++this.length;
***REMOVED***

  _proto.shift = function shift() {
    if (this.length === 0) return;
    var ret = this.head.data;
    if (this.length === 1) this.head = this.tail = null;else this.head = this.head.next;
    --this.length;
    return ret;
***REMOVED***

  _proto.clear = function clear() {
    this.head = this.tail = null;
    this.length = 0;
***REMOVED***

  _proto.join = function join(s) {
    if (this.length === 0) return '';
    var p = this.head;
    var ret = '' + p.data;

    while (p = p.next) {
      ret += s + p.data;
  ***REMOVED***

    return ret;
***REMOVED***

  _proto.concat = function concat(n) {
    if (this.length === 0) return Buffer.alloc(0);
    var ret = Buffer.allocUnsafe(n >>> 0);
    var p = this.head;
    var i = 0;

    while (p) {
      copyBuffer(p.data, ret, i);
      i += p.data.length;
      p = p.next;
  ***REMOVED***

    return ret;
***REMOVED*** // Consumes a specified amount of bytes or characters from the buffered data.
  ;

  _proto.consume = function consume(n, hasStrings) {
    var ret;

    if (n < this.head.data.length) {
      // `slice` is the same for buffers and strings.
      ret = this.head.data.slice(0, n);
      this.head.data = this.head.data.slice(n);
  ***REMOVED*** else if (n === this.head.data.length) {
      // First chunk is a perfect match.
      ret = this.shift();
  ***REMOVED*** else {
      // Result spans more than one buffer.
      ret = hasStrings ? this._getString(n) : this._getBuffer(n);
  ***REMOVED***

    return ret;
***REMOVED***

  _proto.first = function first() {
    return this.head.data;
***REMOVED*** // Consumes a specified amount of characters from the buffered data.
  ;

  _proto._getString = function _getString(n) {
    var p = this.head;
    var c = 1;
    var ret = p.data;
    n -= ret.length;

    while (p = p.next) {
      var str = p.data;
      var nb = n > str.length ? str.length : n;
      if (nb === str.length) ret += str;else ret += str.slice(0, n);
      n -= nb;

      if (n === 0) {
        if (nb === str.length) {
          ++c;
          if (p.next) this.head = p.next;else this.head = this.tail = null;
      ***REMOVED*** else {
          this.head = p;
          p.data = str.slice(nb);
      ***REMOVED***

        break;
    ***REMOVED***

      ++c;
  ***REMOVED***

    this.length -= c;
    return ret;
***REMOVED*** // Consumes a specified amount of bytes from the buffered data.
  ;

  _proto._getBuffer = function _getBuffer(n) {
    var ret = Buffer.allocUnsafe(n);
    var p = this.head;
    var c = 1;
    p.data.copy(ret);
    n -= p.data.length;

    while (p = p.next) {
      var buf = p.data;
      var nb = n > buf.length ? buf.length : n;
      buf.copy(ret, ret.length - n, 0, nb);
      n -= nb;

      if (n === 0) {
        if (nb === buf.length) {
          ++c;
          if (p.next) this.head = p.next;else this.head = this.tail = null;
      ***REMOVED*** else {
          this.head = p;
          p.data = buf.slice(nb);
      ***REMOVED***

        break;
    ***REMOVED***

      ++c;
  ***REMOVED***

    this.length -= c;
    return ret;
***REMOVED*** // Make sure the linked list only shows the minimal necessary information.
  ;

  _proto[custom] = function (_, options) {
    return inspect(this, _objectSpread({***REMOVED***, options, {
      // Only inspect one level.
      depth: 0,
      // It should not recurse.
      customInspect: false
  ***REMOVED***));
***REMOVED***

  return BufferList;
***REMOVED***();